rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Projeto acadêmico / demonstração — aberto. Para produção, use autenticação.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
